testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --verbose
    --tb=short
    --cov=.
    --cov-report=html
    --cov-report=xml
    --cov-report=term-missing
    --cov-exclude=tests/*
    --cov-exclude=alembic/*
    --cov-fail-under=80